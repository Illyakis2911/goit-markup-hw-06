#include <stdio.h>

int main()
{   char C;
    // input N1, N2, C
    switch (N1) 
    {
        case -1 : switch (C){
                  case 'N': C='E'; break; 
                  case 'S': C=
                  case 'E': C=
                  case 'W': C=
                }
                break;
        case 1:

             break;
        case 2:
        break;
    }
    switch (N2)
    {
    case -1:
        switch (C)
        {
        case 'N':C = 'E'; break;
        case 'S': C =
        case 'E' : C =
         case 'W' : C =
        }
        break;
    case 1:

        break;
    case 2:
        break;
    }
printf ("res=%C", C);
    return 0;
}
